# Project Profile

Fill this in once. Every agent reads it before acting and never re-asks.

- Language:
- Framework:
- Database:
- Testing: unit and integration tests are the default expectation for all
  new behavior; manual test-case documents may complement automated tests
  but never replace them
- Run command:
- Conventions:
